//! {{project}}

{% if kind == "lib" -%}
#![deny(missing_docs)]
{% endif -%}


{% if kind == "bin" -%}
fn main() {
    println!("Hello, world!");
}
{% else %}
/// Method to add two variables
pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
{% endif -%}
